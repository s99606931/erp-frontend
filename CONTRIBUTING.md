# 🤝 기여 가이드 (Contributing Guide)

> **문서 목적**: 프로젝트 기여자를 위한 개발 규칙 및 워크플로우
> **마지막 업데이트**: 2025-12-20

---

## 📌 시작하기 전에

> [!IMPORTANT]
> 반드시 [CODING_GUIDELINES.md](docs/task/CODING_GUIDELINES.md)를 먼저 읽어주세요!

### 필수 요구사항

- **Node.js** 20.x 이상
- **pnpm** 8.x 이상 (필수! npm/yarn 금지)
- **Docker** (배포 시)

---

## 🔧 개발 환경 설정

```bash
# 1. 저장소 클론
git clone <repository-url>
cd erp-frontend

# 2. 의존성 설치
pnpm install

# 3. 개발 서버 실행
pnpm dev
```

---

## 📂 프로젝트 구조 이해

```
erp-frontend/
├── apps/shell/           # Shell 앱 (메인 컨테이너)
├── services/             # 마이크로 프론트엔드 서비스
│   ├── auth/web/         # 인증
│   ├── hrm/web/          # 인사관리
│   └── payroll/web/      # 급여관리
├── packages/
│   ├── ui/               # @erp/ui 디자인 시스템
│   ├── shared/           # @erp/shared 공통 로직
│   └── config/           # 공통 설정
└── docs/                 # 문서
```

---

## 🌿 브랜치 전략

| 브랜치 | 용도 |
|--------|------|
| `main` | 프로덕션 배포용 |
| `develop` | 개발 통합 브랜치 |
| `feature/*` | 새 기능 개발 |
| `fix/*` | 버그 수정 |
| `docs/*` | 문서 작업 |

### 브랜치 네이밍

```
feature/T1.2.3-login-form
fix/shell-sidebar-overflow
docs/update-readme
```

---

## 📝 커밋 규칙

### 커밋 메시지 형식

```
<type>(<scope>): <subject>

<body>
```

### Type 종류

| Type | 설명 |
|------|------|
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `docs` | 문서 수정 |
| `style` | 코드 포맷팅 |
| `refactor` | 리팩토링 |
| `test` | 테스트 추가 |
| `chore` | 빌드/설정 |

### 예시

```
feat(auth): 2FA OTP 입력 컴포넌트 추가

- 6자리 OTP 입력 폼 구현
- 자동 포커스 이동
- 붙여넣기 지원
```

---

## ✅ PR 체크리스트

PR 생성 전 반드시 확인:

```bash
# 1. 린트 검사
pnpm lint

# 2. 타입 검사
pnpm typecheck

# 3. 테스트
pnpm test

# 4. 빌드
pnpm build
```

### PR 템플릿

```markdown
## 변경 사항
- 구현한 기능 설명

## 관련 이슈
- closes #123

## 테스트
- [ ] 단위 테스트 통과
- [ ] 타입 검사 통과
- [ ] 린트 통과

## 스크린샷 (UI 변경 시)
```

---

## 🚫 금지 사항

1. **`any` 타입 사용 금지**
2. **인라인 스타일 금지** - Tailwind 클래스 사용
3. **npm/yarn 사용 금지** - pnpm만!
4. **다른 서비스 직접 import 금지**
5. **console.log 커밋 금지**

---

## 📦 패키지 추가

```bash
# 특정 패키지에 의존성 추가
pnpm --filter @erp/ui add lucide-react
pnpm --filter shell add zod

# 루트에 개발 의존성 추가
pnpm add -D -w eslint
```

---

## 🧪 테스트

```bash
# 전체 테스트
pnpm test

# 특정 패키지 테스트
pnpm --filter @erp/ui test

# 커버리지 확인
pnpm test --coverage
```

---

## 📚 참고 문서

- [코딩 지침](docs/task/CODING_GUIDELINES.md)
- [디자인 시스템 규칙](docs/DESIGN_SYSTEM_RULES.md)
- [PRD](docs/PRD_MAIN_UI.md)
- [완료 보고서](docs/task/reports/README.md)
