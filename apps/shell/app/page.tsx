/**
 * ============================================================================
 * 파일명: page.tsx
 * 앱: shell
 * 경로: apps/shell/app/page.tsx
 * 작성일: 2025-12-19
 * ============================================================================
 *
 * [📄 파일 설명]
 * 루트 페이지입니다.
 * 대시보드로 리다이렉트합니다.
 * (인증이 필요한 경우 middleware에서 로그인으로 리다이렉트됨)
 * ============================================================================
 */

import { redirect } from 'next/navigation';

export default function Home() {
    // 대시보드로 리다이렉트 (미인증 시 middleware가 /login으로 처리)
    redirect('/dashboard');
}
